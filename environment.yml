name: intake-axds
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - aiohttp
  - h5netcdf
  - intake
  - intake-xarray
  - pandas
  - pip
  - requests
  - pip:
    - cf-pandas
