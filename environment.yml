name: intake-axds
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - aiohttp
  - cf_pandas
  - h5netcdf
  - pandas
  - pip
  - requests
  - shapely
  - pip:
    - nested_lookup
    - intake
    - intake-parquet
    - intake-xarray
    # - "dask[complete]"
